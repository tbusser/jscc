/**
 * [description]
 * @param  {[type]} ) {	'use        strict';	document.addEventListener('click', function() {		console.log('document clicked');	});} [description]
 * @return {[type]}   [description]
 */
(function() {
	'use strict';

	/**
	 * My comment
	 */
	document.addEventListener('click', function() {
		var context = window.AudioContext || window.webkitAudioContext,
		    elem = document.getElementsByClassName('test'),
			worker = new   Worker ();
			window.getContext( '2d' );
			window.strokeText('Hello!');
		//window.onhashchange = function(){};
		window.addEventListener	(	'hashchange', function(){});
		// Oh noes, a console statement!
		console.log('document clicked');

		var db=	window.openDatabase();
		var db2 = window.indexedDB;
		var es = new EventSource('');
		es.postMessage('test');
		navigator.geolocation.getCurrentPosition();
		var wgl = initWebGL();
		wgl.createShadowRoot();
		= new  WebSocket  ()    ;

		.querySelectorAll('link[rel="import"]');

		elem.classList.add('test');
	});
});
